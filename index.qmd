---
title: "Md Saidul Hoque Anik"
description: "PhD Candidate at Texas A&amp;M | ML Systems & Performance Optimization | Scalable and Differentiable Sparse Kernels for Graph Learning<br><br>Contact: anik[at]tamu[dot]edu"
toc: false
format: 
  html:
    page-layout: full
listing:
  - id: listings-news
    sort-ui: false
    filter-ui: false
    date-format: "MM/YY"
    contents: 
    - news/news.yaml
    - news/posts/**/*.qmd
    type: table
    sort: "date desc"
    fields: [date, caption]
    field-links: [caption]
    include:
      highlight: "true"
  - id: listings-research
    sort-ui: false
    filter-ui: false
    grid-columns: 1
    max-description-length: 300
    date-format: "MMM YYYY" 
    contents: 
    - research/systems
    type: grid
    sort: "date desc" 
    # grid-item-border: false
    # table-hover: true 
    fields: [image, title, description, date]
    # field-links: [caption]
    include:
      featured: "true"
about:
  template: trestles
  image: images/propic2.png 
  image-title: "At MLSys 2025"
  links:
    - icon: github
      text: Github
      href: https://github.com/OnixHoque
    - icon: linkedin
      text: LinkedIn 
      href: https://www.linkedin.com/in/onixhoque/
    - text: "{{< ai google-scholar >}} Google Scholar"
      href: https://scholar.google.com/citations?user=teERpisAAAAJ&hl=en
---

#### Recent News

::: {#listings-news}
:::

## About Me

I am a PhD candidate in Computer Science & Engineering at Texas A&M University, focusing on **high-performance and distributed machine learning systems**, with expertise in **sparse linear algebra, scalable ML infrastructure, and efficient GPU execution**. My work spans **academic research, industry, and national-lab collaboration**, including internships at **Amazon AWS (2025)** and **NVIDIA (2026)** and ongoing collaboration with **Oak Ridge National Laboratory**. I am happy to have been invited to serve on the **Program Committee for MLSys 2027**.

After completing my PhD (tentatively at the end of **Fall 2027**), I plan to pursue **industry roles** as an ML systems engineer or researcher, focusing on building high-performance and scalable machine learning infrastructure.

> I can:
>
> -   Build custom **PyTorch GNN training pipelines** using optimized sparse linear algebra for scalable graph learning.
>
> -   Integrate custom **C++/CUDA kernels and systems APIs** into PyTorch using **LibTorch and PyBind11**.
>
> -   Develop **distributed ML systems** with PyTorch Distributed, including efficient disk-based data streaming for large-scale workloads.
>
> -   Profile and optimize **Python/C++/CUDA pipelines** using **NVIDIA Nsight Systems (NSYS), Nsight Compute (NCU), and IKET** to identify kernel-level performance bottlenecks.
>
> -   Apply **ML and systems optimization techniques** to develop efficient **GPU kernels, MoE execution systems, and performance-tuning infrastructure**, with optimizations deployed in **production ML systems**.

Prior to my PhD, I [taught](/teaching/) undergraduate courses for over **5 years**, including in tenure-track roles, spanning computer systems, theory, and core programming, such as Web Programming, **Computer Architecture**, Theory of Computation, **Programming Languages (C/C++/Java)**, and Data Structures & Algorithms.

## Research Background

My PhD research focuses on **scalable graph learning systems (GNNs, KGEs, and GraphRAG)**, high-performance sparse linear algebra, and efficient ML systems. At **MLSys 2025**, I presented a generalized method for expressing KGE training models through sparse matrix multiplication, achieving up to **5.3× training speedup on CPUs, 4.2× on GPUs, and 11.1× improvement in CUDA memory efficiency**. I also developed a **high-performance CPU SpMM library** (ACM WebConf 2024) that accelerates PyTorch GNN training by up to **93× across Intel, AMD, and ARM CPUs**.

During my **Amazon Summer Internship in 2025**, I developed a **differentiable GPU kernel autotuner for MoE kernels**, using transfer learning to improve prediction of optimal kernel configurations and reduce tuning time from days to hours.

During my **NVIDIA internship in 2026**, I worked with the cuDNN/FlashInfer team on **MoE execution APIs and tuning infrastructure**, with the resulting extensions integrated into **FlashInfer v0.6.16**. I also conducted **system- and kernel-level performance analysis** using Nsight Systems, Nsight Compute, and IKET to identify bottlenecks in **CuTe DSL kernels and vLLM serving systems**.

Currently, I am collaborating with **Oak Ridge National Laboratory** on a distributed, differentiable framework for **large-scale KGE training across hybrid compute tiers**.

<!-- :::{.column-margin} -->

## Featured Research

::: {#listings-research}
:::

> Discover all 20+ [research projects](/research/index.qmd) or view the full list of [publications](/news/#category=Publication)

```{=html}
<!-- * [Amazon AWS Internship 2025: Developed Differentiable GPU Kernel Autotuner.](/research/systems/gpu-tuner-amazon/index.qmd) 
* [MLSys 2025 Paper: Generalized 10 KGE model training using faster sparse-dense matrix multiplication kernel.](/research/systems/sptransx/index.qmd)
* [SC24 best poster finalist (top-6): Expressed TransE Knowledge Graph Embedding (KGE) training using faster sparse-dense matrix multiplication kernel.](/research/systems/efficient-kge/index.qmd)
 -->
```

## Leadership Roles

I **led** and **coordinated** various academic and technical initiatives, including **curriculum revisions**, **postgraduate programs**, and **programming contests** at UIU, MIST, and BUET, fostering a culture of **innovation** and academic excellence across departments. See all my leadership roles and initiatives [here](/leadership/).

## Exploration

I deepen my **ML systems** expertise by solving [LeetGPU](/leetgpu/) problems across **PyTorch**, **CUDA**, and **Triton**, building intuition for low-level **kernel design** and **performance trade-offs**. I stay current with **ML infrastructure** and **systems trends** by developing small experimental tools and continuously expanding my [software](/software/) skill set, with **12+ open-source** and commercial projects in **Python**, **C++**, and **Java**. I also regularly read and distill recent **ML systems papers**, sharing relevant hands-on tutorials and technical walkthroughs on my [blog](/blog/) to communicate systems concepts clearly and effectively.

<!-- I deepen my ML systems expertise by solving [LeetGPU](/blog/#category=LeetGPU) problems across PyTorch, CUDA, and Triton, developing intuition for performance trade-offs and low-level kernel design. I stay current with ML infrastructure and systems trends by building small experimental tools and continuously expanding my [software](/blog/#category=LeetGPU) skill set. -->

<!-- ::: -->

```{=html}
<script>
  document.addEventListener('DOMContentLoaded', () => {
  const section = document.getElementById('recent-news');
  // const section = document.getElementById('highlights');
  const targetDiv = document.querySelector('.about-entity');
  if (section && targetDiv) {
    section.remove();
    targetDiv.appendChild(section);
    section.style.textAlign = 'left';
    section.style.fontSize = '0.95em';
    const table = section.querySelector('table');
    if (table) {
      const thead = table.querySelector('thead');
      if (thead) {
        thead.remove();
      }
    }
    table.insertAdjacentHTML('afterend', '<div style="text-align: right;"><a href="/news/">See All ⟶ </a></div>');
  }

  function applyLayout() {
    document.querySelectorAll('.quarto-grid-item.card').forEach(function(card) {
      const imgP = card.querySelector('p.card-img-top');
      if (!imgP) return;
      
      const img = imgP.querySelector('img');
      const body = card.querySelector('.card-body');
      if (!img || !body) return;
      
      // Style image to fit container entirely without cropping
      img.style.maxWidth = '100%';
      img.style.maxHeight = '100%';
      img.style.width = 'auto';
      img.style.height = 'auto';
      img.style.objectFit = 'contain'; // Scale to fit within container, preserving aspect ratio
      img.style.display = 'block';

      if (window.innerWidth < 768) return; // Skip on mobile (< md breakpoint)
 
      // Remove original image paragraph and extract img
      card.removeChild(imgP);
      
      // Use flexbox for reliable horizontal layout
      card.style.display = 'flex';
      card.style.flexDirection = 'row';
      card.style.overflow = 'hidden';
      // card.style.alignItems = 'center';
      
      // Create a square container for the image
      const imgContainer = document.createElement('div');
      imgContainer.style.width = '150px'; // Fixed side length for square; adjust as needed
      imgContainer.style.height = '150px';
      imgContainer.style.flexShrink = '0'; 
      imgContainer.style.overflow = 'hidden';
      imgContainer.style.marginLeft = '0.5rem'; 
      // imgContainer.style.marginRight = '0.5rem'; 
      imgContainer.style.display = 'flex';
      imgContainer.style.alignItems = 'center'; // Vertical centering
      imgContainer.style.justifyContent = 'center'; // Horizontal centering
      // imgContainer.style.backgroundColor = '#f8f9fa'; // Optional: light bg for letterboxing
      
      
      
      // Append img to container, then container to card
      imgContainer.appendChild(img);
      card.insertBefore(imgContainer, body);
      
      // Body takes remaining space
      body.style.flex = '1';
      body.style.display = 'flex';
      body.style.flexDirection = 'column';
      body.style.justifyContent = 'space-between';
      body.style.padding = '10px';

      // Zero out margin-bottom on the description p
      const descP = body.querySelector('.listing-description p');
      if (descP) descP.style.marginBottom = '0';
    }); 
  }
  applyLayout(); // Initial apply on load
  
  // Re-apply on resize (debounced for performance)
  let resizeTimeout;
  window.addEventListener('resize', function() {
    clearTimeout(resizeTimeout);
    resizeTimeout = setTimeout(applyLayout, 250);
  });
  
  // Optional: Listen to media query changes for more precision
  const mediaQuery = window.matchMedia('(min-width: 768px)');
  mediaQuery.addEventListener('change', applyLayout);

});
</script>
```